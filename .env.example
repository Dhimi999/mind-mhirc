# ═══════════════════════════════════════════════════════════
# ENVIRONMENT VARIABLES CONFIGURATION
# ═══════════════════════════════════════════════════════════

# ───────────────────────────────────────────────────────────
# SUPABASE CONFIGURATION (REQUIRED)
# ───────────────────────────────────────────────────────────
# Get these from: https://supabase.com/dashboard → Project Settings → API

VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# ───────────────────────────────────────────────────────────
# OPTIONAL: SERVICE ROLE KEY (for server-side operations)
# ───────────────────────────────────────────────────────────
# Only needed for Vercel serverless functions (sitemap generation)
# WARNING: Never expose this key in client-side code!

SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# ───────────────────────────────────────────────────────────
# OPTIONAL: FORCE SPECIFIC BASE URL
# ───────────────────────────────────────────────────────────
# Overrides auto-detection of domain in sitemap generation
# Recommended for production deployments

SITE_BASE_URL=https://mind-mhirc.my.id

# ═══════════════════════════════════════════════════════════
# SETUP INSTRUCTIONS
# ═══════════════════════════════════════════════════════════

# LOCAL DEVELOPMENT:
# 1. Copy this file to .env
#    cp .env.example .env
# 
# 2. Replace placeholder values with actual credentials from Supabase Dashboard
#    - Go to: https://supabase.com/dashboard
#    - Select your project
#    - Settings → API
#    - Copy URL and anon key
# 
# 3. Run dev server
#    npm run dev

# VERCEL DEPLOYMENT:
# 1. Go to: https://vercel.com/dashboard
# 2. Select your project → Settings → Environment Variables
# 3. Add these variables:
#    - VITE_SUPABASE_URL (Production, Preview, Development)
#    - VITE_SUPABASE_ANON_KEY (Production, Preview, Development)
#    - SUPABASE_SERVICE_ROLE_KEY (Production only - for sitemap)
#    - SITE_BASE_URL (Production only - for canonical URLs)
# 4. Redeploy your application

# ═══════════════════════════════════════════════════════════
# SECURITY NOTES
# ═══════════════════════════════════════════════════════════

# ✅ SAFE to expose in browser (prefix with VITE_):
#    - VITE_SUPABASE_URL
#    - VITE_SUPABASE_ANON_KEY
#    - SITE_BASE_URL

# ⚠️ NEVER expose in browser (server-side only):
#    - SUPABASE_SERVICE_ROLE_KEY

# 🔒 Never commit .env file to Git (already in .gitignore)
